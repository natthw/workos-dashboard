/** @type {import('next').NextConfig} */
const nextConfig = {
  // The dashboard reads/writes markdown in the WorkOS vault on the server only;
  // no special config needed. Kept minimal on purpose.
};

export default nextConfig;
